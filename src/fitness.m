function vector = fitness(chromosomes, sources, goal_power)
    cur_pop = length(chromosomes);
    vector = zeros(cur_pop, 1);

    power = 1;
    cost = 2; 
    emission = 3;
    for i = 1:cur_pop
        f1 = 
        f2 = 
        f3 = 
        vector(i) = (f1 + f2 + f2)/length(boundaries);  
    end

end